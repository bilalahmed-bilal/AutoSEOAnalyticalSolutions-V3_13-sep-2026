import type { NextRequest } from "next/server";
import { supabaseRest } from "@/lib/db/supabase-rest";
export interface ContentStrategyProject {id:string;workspaceId:string;name:string;keywordProjectId?:string;competitorProjectId?:string;status:string;summary:any;strategy:any;createdAt:string;updatedAt:string;}
const q=(p:Record<string,string>)=>"?"+Object.entries(p).map(([k,v])=>`${k}=${encodeURIComponent(v)}`).join("&");
const map=(r:any):ContentStrategyProject=>({id:r.id,workspaceId:r.workspace_id,name:r.name,keywordProjectId:r.keyword_project_id??undefined,competitorProjectId:r.competitor_project_id??undefined,status:r.status,summary:r.summary||{},strategy:r.strategy||{},createdAt:r.created_at,updatedAt:r.updated_at});
export async function createContentStrategy(ctx:{req:NextRequest;workspaceId:string},input:{name:string;keywordProjectId?:string;competitorProjectId?:string;summary?:any;strategy?:any;createdBy?:string}){const [r]=await supabaseRest<any[]>(ctx.req,"content_strategy_projects",{method:"POST",body:JSON.stringify({workspace_id:ctx.workspaceId,name:input.name,keyword_project_id:input.keywordProjectId||null,competitor_project_id:input.competitorProjectId||null,status:"ready",summary:input.summary||{},strategy:input.strategy||{},created_by:input.createdBy||null}),headers:{Prefer:"return=representation"}});return map(r);}
export async function listContentStrategies(ctx:{req:NextRequest;workspaceId:string}){const rows=await supabaseRest<any[]>(ctx.req,"content_strategy_projects",{},q({workspace_id:`eq.${ctx.workspaceId}`,order:"created_at.desc"}));return rows.map(map);}
export async function getContentStrategy(ctx:{req:NextRequest;workspaceId:string},id:string){const rows=await supabaseRest<any[]>(ctx.req,"content_strategy_projects",{},q({workspace_id:`eq.${ctx.workspaceId}`,id:`eq.${id}`,limit:"1"}));return rows[0]?map(rows[0]):null;}
