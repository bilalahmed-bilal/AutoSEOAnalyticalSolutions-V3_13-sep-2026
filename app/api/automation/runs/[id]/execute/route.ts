import { NextRequest } from "next/server";
import { requireApiAccess, unauthorizedResponse } from "@/lib/auth/api-access";
import { getTenantContext } from "@/lib/tenant";
import { getWorkflow, listRuns, updateRun } from "@/lib/automation-repository";
import { executeWorkflowSteps } from "@/lib/automation-executor";
export async function POST(req:NextRequest,{params}:{params:Promise<{id:string}>}){const a=await requireApiAccess(req);if(!a)return unauthorizedResponse();const t=await getTenantContext(req);if(!t)return Response.json({error:"Workspace access required."},{status:403});const id=(await params).id;const runs=await listRuns({req,workspaceId:t.workspaceId});const run=runs.find(x=>x.id===id);if(!run)return Response.json({error:"Run not found."},{status:404});const w=await getWorkflow({req,workspaceId:t.workspaceId},run.workflowId);if(!w)return Response.json({error:"Workflow not found."},{status:404});await updateRun({req,workspaceId:t.workspaceId},id,{status:"running"});try{const results=await executeWorkflowSteps({req,workspaceId:t.workspaceId},w.steps,run.input);const failed=results.some(x=>x.status==="failed");const saved=await updateRun({req,workspaceId:t.workspaceId},id,{status:failed?"failed":"succeeded",results,error:failed?"One or more workflow steps failed.":undefined});return Response.json({run:saved,results});}catch(e:any){const saved=await updateRun({req,workspaceId:t.workspaceId},id,{status:"failed",results:[],error:String(e?.message||e)});return Response.json({run:saved,error:e.message||"Workflow execution failed."},{status:400});}}
